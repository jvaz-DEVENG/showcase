<UserControl x:Class="GameBoost.App.Views.JogosPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      Pagina Jogos (secoes 5.1 e 5.11): biblioteca dos launchers cruzada com
      os perfis gravados. Um jogo sem perfil aparece igual aos outros: o
      perfil nao e obrigatorio para nada.
    -->
    <Grid Margin="26,22,26,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding Titulo}" FontSize="22" FontWeight="Bold" />
                <TextBlock Text="{Binding Subtitulo}" Foreground="{StaticResource TextoSecundario}"
                           Margin="0,3,12,0" TextWrapping="Wrap" />
            </StackPanel>

            <Button Grid.Column="1" Content="Varrer" Style="{StaticResource BotaoBase}"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Varrer biblioteca de jogos"
                    Command="{Binding VarrerCommand}"
                    IsEnabled="{Binding Ocupado, Converter={StaticResource InverterBool}}" />
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <Border Background="{StaticResource Superficie}" CornerRadius="6" Padding="14,10">
                <TextBlock Text="{Binding Resumo}" FontWeight="SemiBold" TextWrapping="Wrap" />
            </Border>

            <Border Background="{StaticResource Superficie}" CornerRadius="6" Padding="14,10" Margin="0,8,0,0"
                    Visibility="{Binding JogoEmExecucao, Converter={StaticResource TextoParaVisibilidade}}">
                <TextBlock Text="{Binding JogoEmExecucao}" Foreground="{StaticResource Acento}" TextWrapping="Wrap" />
            </Border>
        </StackPanel>

        <Border Grid.Row="2" Background="{StaticResource Superficie}"
                BorderBrush="{StaticResource Borda}" BorderThickness="1" CornerRadius="8">
            <Grid>
                <ListBox ItemsSource="{Binding Jogos}" SelectedItem="{Binding Selecionado}"
                         Background="Transparent" BorderThickness="0" Padding="6"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="6,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Nome}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Detalhe}" FontSize="12"
                                               Foreground="{StaticResource TextoSecundario}"
                                               TextWrapping="Wrap" />
                                </StackPanel>

                                <Border Grid.Column="1" Background="{StaticResource Borda}" CornerRadius="3"
                                        Padding="6,1" VerticalAlignment="Center"
                                        Visibility="{Binding TemPerfil, Converter={StaticResource BoolParaVisibilidade}}">
                                    <TextBlock Text="Perfil" FontSize="10"
                                               Foreground="{StaticResource TextoSecundario}" />
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock Text="Clique em Varrer para ler a sua biblioteca."
                           Foreground="{StaticResource TextoSecundario}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="{Binding ListaVazia, Converter={StaticResource BoolParaVisibilidade}}" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,12,0,18">
            <StackPanel Orientation="Horizontal">
                <Button Content="Criar perfil" Style="{StaticResource BotaoPrimario}"
                        Command="{Binding CriarPerfilCommand}" />
                <Button Content="Ativa sozinho" Style="{StaticResource BotaoBase}" Margin="8,0,0,0"
                        Command="{Binding AlternarAutomaticoCommand}" />
                <Button Content="Remover perfil" Style="{StaticResource BotaoBase}" Margin="8,0,0,0"
                        Command="{Binding RemoverPerfilCommand}" />
            </StackPanel>

            <TextBlock Text="{Binding Status}" Foreground="{StaticResource TextoSecundario}"
                       FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap" MaxHeight="52" />
        </StackPanel>
    </Grid>
</UserControl>
